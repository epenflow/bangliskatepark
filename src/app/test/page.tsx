'use client';
import Finder from '@/components/Section/Gallery/Finder';
import React from 'react';
import { createPortal } from 'react-dom';

const Page = () => {
	return (
		<div
			id='test'
			className='relative flex items-center flex-row h-screen w-screen'></div>
	);
};

export default Page;
